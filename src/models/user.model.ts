export interface RegisterUserModal {
  userFirstName: string;
  userLastName: string;
  userEmail: string;
  userPassword: string;
}

export interface LoginUserModal {
  userEmail: string;
  userPassword: string;
}
